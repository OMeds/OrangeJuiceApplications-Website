# Contact page overrides

- Top: 3 contact channel cards (support, privacy, intake) with icons — no feedback form on this page
- Hero: single H1 "Contact" + actions (Start a project, Email support)
- Prose: intro, response times table, project intake link only (no duplicate email block)
- Calendly card: user-facing copy only — never reference site-config.js or dev setup
- Footer CTA: Start a project + Email us
